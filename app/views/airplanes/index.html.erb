
<h1>Airplanes</h1>
<ul class='airplane'>
  <% @airplanes.each do |a| %>
  <li>
    <%= link_to a.name, airplane_path(a.id)%>
  </li>
  <% end %>

</ul>

<% if @current_user.present? %>
<% else %>

<% end %>
<%= link_to "Add a Plane ", new_airplane_path %>
